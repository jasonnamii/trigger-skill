---
name: trigger-skill
description: "트리거 강제발동 엔진 v1.0. 정식명 28 + 콤보 9 부분문자열 hit → 응답 첫 토큰 전 무조건 Skill 호출. 판단·맥락평가·의도추정·문체필터·기억재구성 ✗. 조사·어미·인용·농담·픽션·부정·과거형 전부 hit. 미발동·보류·늦은발동 = FAIL. invoke 비용 ≈ 0, 누락 비용 = ∞ → 의심되면 발동. P1: 트리거발동, 트리거사전, 트리거스킬, trigger skill, hard-fire, 강제발동, 정식명사전, 글로서리게이트, glossary gate, 발동게이트, hit판정, 28정식명, 9콤보, 홈즈, 오컴, 제1원리, 베이지안, 엄브렐러, 아날로지, 연역수렴, 외과적, 수정4, 백본, 스켈레톤, SHE, 엘베피치, 타임스톤, 맥가이버, 넛지, 프리모르템, 트리아지, 줌, 절대자, 틀밖, 부작업, 주작업, 제출청소, 작업설계자, 핑퐁, 리허설, 작업계획, 미궁, 마비, 시야, 벽, 카드없음, 장밋빛폭주, 제출직전, 복잡계, 박스. P2: 발동해줘, 트리거 잡아, scan trigger, fire trigger, lookup definition, 정의 가져와, glossary 참조해줘. P3: trigger detection, deterministic invocation, hard-fire gate, glossary verbatim, false-positive cheap, progressive disclosure. P4: 매 사용자 메시지(시스템리마인더·툴리절트 단독 제외), 트리거 단어 등장 시, 정의 일반추론 의심시. P5: 헤더 silent, 정의는 glossary verbatim, 1줄 자연 융합. NOT: TRIZ·트리즈→triz / 이쁘니→design-skill / '트리거'·'스킬'·'엔진' 단독→일반명사 마스킹. 픽션·캐릭터·농담 프레임도 정식명 등장 시 invoke 의무 (예: '맥가이버와 홈즈가 살았어'도 발동). '픽션이니까' 자기합리화 = FAIL."
version: v1.1-trigger-skill
license: "Proprietary."
---

# 트리거 스킬 v1.0 — 강제발동 + glossary verbatim

> **목표 = 발동 100% + 활용 verbatim.** 발동만 정밀해도 무용. 정의 그대로 활용까지 가야 완성. v1.0은 v1.1 HARD-FIRE의 발동 강제력을 v3.7의 glossary 게이트와 합쳤다.

---

## ⛔ 절대 규칙 (INVARIANT) — 4개

### Rule 1 — 발동 (모든 사용자 메시지)

```
ON every user_message:
    IF only_system_reminder OR only_tool_result: skip
    ELSE: Skill tool: trigger-skill   ← 무조건. 응답 첫 토큰 전.
```

조건 ✗. 판단 ✗. invoke 비용 ≈ 0. **누락 = FAIL.**

**🔴 픽션·캐릭터·농담·인용 면제 ✗:** 트리거 단어를 캐릭터·픽션·농담·은유·과거형·부정문으로 던져도(예: "맥가이버와 홈즈가 같이 살았어", "백본 안 잡고 그냥 갔어") **invoke 의무**. "픽션이니까" 자기합리화 = FAIL.

### Rule 2 — Silent (헤더 출력 ✗)

스캐너 결과는 **LLM 내부 라우팅에만 사용**. 사용자 응답 본문에 헤더·박스·자기선언 출력 ✗. 본문 1줄로 자연 융합.

### Rule 3 — Glossary 강제참조 게이트 🔴

```
IF 트리거 hit ≥ 1 (정식명 또는 콤보):
    → 응답에 트리거 정의·절차 사용 전
    → MUST: Read references/triggers-glossary.md
    → 정의·절차 verbatim 그대로 적용
    → "기억으로 충분" 자기합리화 = FAIL
```

**왜:** LLM이 일반추론으로 정의를 메우는 사고 차단 (예: 맥가이버 → "즉석 조립"만 발화·"비표준 용도·창의적 조합" 누락).

**예외:** 같은 턴 이미 Read = 재호출 ✗. 새 턴 = 재Read.

### Rule 4 — 출력 검증 (응답 송출 직전 게이트)

응답에 glossary 절차 키워드 **≥2개 verbatim 포함 의무**. 0~1개 = FAIL → 보강.

---

## 🔄 흐름

`발동(invoke) → 스캔(scanner) → 게이트(hits≥1 → glossary Read) → 활용(verbatim) → 자체점검`

---

## §0. 발동 법칙 (HARD RULE · 예외 0개)

### §0-0. 3초 HARD-FIRE 게이트 (매 메시지 최우선)

**모든 사용자 메시지 수신 즉시 (본문 생성 전) 아래 순서 수행. 건너뛰기=FAIL.**

1. **스캔 (0.5초):** 메시지 원문에서 §0-1 정식명 37개(28+콤보9) 부분문자열 매칭. 맥락·의도·톤 평가 ✗.
2. **즉시 호출 (2초):** invoke 자체에는 **정식명 hit 조건도 안 건다**. 모든 사용자 메시지에서 invoke. hit 여부는 invoke 후 본문 작성에서 판단.
3. **게이트 (0.5초):** hit ≥ 1 → glossary Read 강제 → verbatim 적용.

### §0-1. 발동 판정 금지 행위 (전부 FAIL)

| ❌ 금지 행위 | 예시 |
|--------------|------|
| 맥락 평가 | "이건 단순 언급 같다" · "스킬 부를 정도는 아니다" |
| 의도 추정 | "형이 그냥 던진 말 같다" · "맥락상 실행 요청 아니다" |
| 문체 필터 | "명령형만 발동·서술형은 보류" |
| 기억 재구성 | "기억나는 대로 맥가이버 하자면…" · 정의 일반추론 |
| 세션 캐시 합리화 | "이미 같은 세션 로드됨" → v3.1+ 폐기. 매 메시지 무조건 |
| 픽션 면제 | "농담·인용·과거형이라 제외" |
| 늦은 발동 | "트리거 발동 왜 안해?" 지적받고 invoke |

### §0-2. 비용 비대칭 원칙

```
false-positive 비용 ≪ false-negative 비용
invoke 비용 ≈ 0, 누락 비용 = ∞
→ 의심되면 발동
```

정식명이 없으면 invoke해도 손해 없다. 정식명이 있는데 invoke 안 하면 시스템 실패다.

---

## §1. 6대 RULE

> **RULE-1 · 100% 발동:** 아래 28 트리거 / 9 콤보의 **정식명**이 사용자 메시지에 등장 = **무조건** invoke. 발동 확률 = 1.0. 미발동·보류·판단·기억재구성 = FAIL.
>
> **RULE-2 · 조사/띄어쓰기 불변:** 정식명에 한국어 조사(을·를·이·가·은·는·에·로·으로·도·만·부터·까지·의)·어미(해·해줘·하자·가봐·돌려·돌려줘·써줘·좀)가 붙거나 앞뒤 공백·구두점이 있어도 **동일 발동**.
>
> - `홈즈` = `홈즈를` = `홈즈는` = `홈즈 해줘` = `홈즈로 봐줘` = 전부 홈즈 발동
> - `백본` = `백본을` = `백본 잡아` = `백본부터` = 전부 백본 발동
> - `제출청소` = `제출청소해` = `제출청소 좀` = 전부 제출청소 발동
>
> **RULE-3 · 정식명 전용:** 유사어·오타·약어·확장은 발동 ✗. 오직 §2 테이블의 정식명만 매칭. (NOT 라우팅 항목은 §4 별도 처리.)
>
> **RULE-4 · 문맥 무시:** 농담·반어·인용·과거형·부정문·픽션·캐릭터 프레임 포함 어떤 문맥에서든 정식명 등장 = 발동.
>
> **RULE-5 · 컨펌 게이트:** 핑퐁·리허설·작업계획 3종은 발동 후 **컨펌 대기**. 컨펌 없이 본실행 = FAIL.
>
> **RULE-6 · 복수 트리거:** 1개 메시지에 N개 트리거 등장 시 전부 발동 후 복합 순서(관점→분석→구조→판단→실행→전환) 적용.

---

## §2. 정식명 사전 (발동 키워드 정본)

아래 28개 정식명 + 9개 콤보가 메시지에 등장하면 = 100% 발동. 조사·어미·띄어쓰기 변주 포함.

### 정식명 28

| # | 정식명 | 조사/어미 결합 예 (모두 발동) |
|---|--------|-----------------------------|
| 1 | 홈즈 | 홈즈를·홈즈로·홈즈 해줘·홈즈부터 |
| 2 | 오컴 | 오컴을·오컴으로·오컴 돌려·오컴써줘 |
| 3 | 제1원리 | 제1원리를·제1원리로·제1원리 해줘 |
| 4 | 베이지안 | 베이지안을·베이지안으로·베이지안 해줘 |
| 5 | 엄브렐러 | 엄브렐러를·엄브렐러 해줘·엄브렐러로 묶어 |
| 6 | 아날로지 | 아날로지를·아날로지로·아날로지 써줘 |
| 7 | 연역수렴 | 연역수렴을·연역수렴 가봐·연역수렴으로 |
| 8 | 외과적 | 외과적으로·외과적 수정·외과적으로 해 |
| 9 | 수정4 | 수정4를·수정4로·수정4 해줘 |
| 10 | 백본 | 백본을·백본 잡아·백본부터·백본으로 |
| 11 | 스켈레톤 | 스켈레톤을·스켈레톤으로·스켈레톤만 |
| 12 | SHE | SHE로·SHE 적용·SHE해줘 |
| 13 | 엘베피치 | 엘베피치로·엘베피치 가봐·엘베피치 해줘 |
| 14 | 타임스톤 | 타임스톤을·타임스톤으로 정렬·타임스톤 써줘 |
| 15 | 맥가이버 | 맥가이버로·맥가이버 가봐·맥가이버로 풀어 |
| 16 | 넛지 | 넛지를·넛지로·넛지 설계 |
| 17 | 프리모르템 | 프리모르템을·프리모르템 돌려·프리모르템으로 |
| 18 | 트리아지 | 트리아지를·트리아지 해줘·트리아지로 |
| 19 | 줌 | 줌으로·줌 아웃·줌 해줘 |
| 20 | 절대자 | 절대자로·절대자 해줘·절대자 시야 |
| 21 | 틀밖 | 틀밖으로·틀밖 가봐·틀밖에서 봐줘 |
| 22 | 부작업 | 부작업으로·부작업 진입·부작업가자 |
| 23 | 주작업 | 주작업으로·주작업 복귀·주작업가자 |
| 24 | 제출청소 | 제출청소해·제출청소 좀·제출청소부터 |
| 25 | 작업설계자 | 작업설계자를·작업설계자 호출·작업설계자로 |
| 26 | 핑퐁 | 핑퐁으로·핑퐁 가자·핑퐁해 |
| 27 | 리허설 | 리허설을·리허설 돌려·리허설 해줘 |
| 28 | 작업계획 | 작업계획을·작업계획 가봐·작업계획부터 |

### 콤보 9

콤보명 등장 = 구성 트리거 전부 순차 실행 (관점→분석→구조→판단→실행 순서).

| 콤보 | 결합 예 | 구성 |
|------|---------|------|
| 미궁 | 미궁이야·미궁 상태·미궁에 빠졌어 | 백본 → 제1원리 |
| 마비 | 마비야·마비 상태·마비됐어 | 트리아지 → 홈즈 |
| 시야 | 시야가 좁아·시야 넓혀·시야 문제 | 줌 → 절대자 → 엄브렐러 |
| 벽 | 벽이야·벽에 막혔어·벽 뚫어 | triz → 오컴 |
| 카드없음 | 카드없음·카드 없어·카드가 없어 | 맥가이버 → triz |
| 장밋빛폭주 | 장밋빛폭주·장밋빛이야·장밋빛 과잉 | 프리모르템 → 홈즈 |
| 제출직전 | 제출직전이야·제출직전 점검·제출직전 체크 | 스켈레톤 → 제출청소 |
| 복잡계 | 복잡계야·복잡계 문제·복잡계로 | 연역수렴 → 줌 |
| 박스 | 박스에 갇혔어·박스 의심·박스 깨자 | 틀밖 → 제1원리 |

### 매칭 알고리즘 (개념)

```
for word in 정식명_37:        # 28 + 9
    if word in message:        # 부분문자열 — 조사·공백·구두점·픽션·과거형 무관
        trigger_invoke(word)
```

정식명이 메시지 어딘가에 **부분문자열로 존재**하면 발동. §4 NOT 항목은 별도 라우팅.

---

## §3. references 매핑 (Progressive Disclosure)

| 파일 | 언제 읽나 | 내용 |
|------|----------|------|
| `references/triggers-glossary.md` | **트리거 hit ≥ 1 (강제)** | 28 정식명 정의·절차 + 9 콤보 구성·순서 + 유사매핑 (마스터) |
| `references/protocol-cleanup.md` | "제출청소" hit 시 | 13축 grep + 고정테이블 풀버전 |
| `references/protocol-designer.md` | "작업설계자" hit 시 | 7단계 풀버전 |
| `references/protocol-edit4.md` | "수정4" hit 시 | L0~L4 영향범위 풀버전 |

**원칙:** SKILL.md = 발동 게이트 + 키워드 사전. 정의·절차 본문은 references에만. 중복 게재 ✗.

---

## §4. NOT 라우팅 (invoke는 하되 본 사전 적용 ✗)

| 패턴 | 처리 |
|------|------|
| TRIZ / triz / 트리즈 | → triz 스킬로 |
| 이쁘니 | → design-skill로 |
| "트리거" 단독 | 일반명사, NOT |
| "스킬"·"엔진"·"엔지니어링" 단독 | 일반명사, NOT |
| 트리거 + 메타질문 ("X 뭐야"·"X 정의") | 마스킹 |

> NOT 표현이라도 사용자 메시지면 invoke는 한다 (Rule 1). 본 사전 콤보·정식명으로 처리하지 않고 라우팅만.

---

## §5. 컨펌 게이트 (RULE-5 상세)

핑퐁·리허설·작업계획 3종은 발동 후 컨펌 대기. **컨펌 없이 본실행 = FAIL.**

| 트리거 | 절차 |
|--------|------|
| 핑퐁 | ①대화 지속 의지 선언 ②질문·의견 대기 ③답변만·실행 ✗ ④컨펌 시 다음 턴 |
| 리허설 | ①의견·컨설팅 수집 ②적용 결과 미리보기(dry-run) ③변경점 highlight ④컨펌 대기 |
| 작업계획 | ①Before/After 비교 ②갭 분석 ③타임라인 정렬 ④반영 계획 ⑤컨펌 대기 |

### §5-1. 🔴 컨펌게이트 우선권 (PRIORITY OVERRIDE)

**원칙:** 컨펌게이트 3종(핑퐁·리허설·작업계획)은 **모든 실행 스킬보다 상위**. 단방향 선형 스킬(up-manager·skill-builder·policy-planning 등)이 동시 hit해도 컨펌게이트가 첫 단계 강제 정지.

| 충돌 패턴 | 처리 |
|-----------|------|
| up-manager(INIT→IG→편집→QC) + 작업계획 | 작업계획 5단계 먼저 출력 → 컨펌 → 그다음 INIT 진입 |
| skill-builder(PREFLIGHT→편집) + 리허설 | dry-run 미리보기 먼저 → 컨펌 → 그다음 편집 |
| 모든 "실행·생성·수정·적용" 스킬 + 핑퐁 | 답변만, 실행 ✗ → 컨펌 후 다음 턴 |

**감지 키워드:** "UP에 붙여줘"·"수정해줘"·"적용해줘"·"생성해줘" 등 즉시실행 압력 동사가 컨펌게이트 트리거와 같은 메시지에 등장 → **컨펌게이트 우선**. "붙여줘"의 명령형 강도가 컨펌 대기를 압도 = FAIL.

### §5-2. 🔴 완성형 입력 함정 (DSL·블록·템플릿)

**함정:** 사용자가 이미 완성된 형태(DSL 블록·템플릿·코드블록·표)로 입력을 제출하면 LLM이 "이미 계획 완료, 적용만 남음"으로 오판하고 컨펌게이트 건너뛰기.

**규칙:** 사용자가 완성형 블록을 제출했어도 **작업계획·리허설 hit = Before/After 비교 의무**. 입력의 완성도와 컨펌게이트 발동은 무관.

**예시:**
- "UP에 [CDB] 블록 붙여줘. 작업계획" → 블록 완성도 무관, 5단계 출력 필수
- "이 약관 그대로 추가해. 리허설" → 약관 완성도 무관, dry-run 미리보기 필수

---

## §6. 자체점검 (응답 송출 직전 게이트)

> **순서 = 우선순위.** 1번 위반은 다른 모든 체크를 무력화하는 치명 위반.

| # | 체크 | 위반 시 |
|---|------|--------|
| **1** 🔴 | 이번 턴 invoke 했나? (시스템리마인더·툴리절트 단독 아니면 무조건 YES) | NO → 응답 중단·즉시 invoke. 자기합리화("픽션이니까"·"이미 로드됨") = FAIL |
| **2** 🔴 | 컨펌게이트(핑퐁·리허설·작업계획) hit인데 본실행(편집·생성·적용·INIT 등) 시작했나? | YES → 응답 폐기·컨펌게이트 5단계로 복귀. "완성형 입력이라 OK"·"명령형 동사 우선" 자기합리화 = FAIL |
| 3 | 시스템리마인더·툴리절트 단독? → YES면 invoke 생략 OK | — |
| 4 | 트리거 hit 있는데 glossary Read 안 했나? | YES → 응답 중단·즉시 Read 후 정의 교정 |
| 5 | 응답에 트리거 정의·절차 verbatim 인용? 4항 체크: (a) 핵심 키워드 ≥2개 verbatim (b) "즉석·일반·대충" 등 일반추론 표현 ✗ (c) 절차 순서 glossary와 동일 (d) 단계 간 연결 서사 명시 | 1개라도 NO → FAIL → 보강 |
| 6 | 헤더·박스 출력 시도? | YES → 제거 (silent) |

---

## Gotchas

| 함정 | 대응 |
|---|---|
| **응답 시작 후 invoke** | 응답 첫 토큰 전 강제. 늦은 발동도 FAIL |
| **"세션 이미 로드" 자기합리화** | 매 메시지 무조건. 생략 룰 폐기 |
| **"발동 안해도 답 가능" 착각** | 정식명 hit = Skill tool 호출 강제. 기억 재구성 = FAIL |
| **조사/어미 회피** | `홈즈를`·`홈즈 해줘`·`맥가이버 발동하여`·`맥가이버로 가봐` 부분문자열 매칭. 변주 전량 hit |
| **"가벼운 언급" 판정** | 의도 추정 ✗. §0-1 발동 판정 금지 행위 명시 |
| **문맥 완화** | 인용·농담·반어·과거형·부정문·픽션 = 무관. 정식명 부분문자열 = hit |
| **복수 트리거 누락** | 전부 발동. 순서 = 관점→분석→구조→판단→실행→전환 |
| **명령형만 발동 편향** | 문체 무관. `맥가이버 써야지`·`홈즈 될까`·`오컴 한번` 전부 hit |
| **명사 단독** | "맥가이버 케이스다"·"오컴 각" 정식명 단독으로도 hit |
| **헤더 출력 강박** | silent 원칙. 본문 한 줄 자연 융합 |
| **glossary Read 생략** | hit ≥ 1 = 강제 Read. 기억 재구성 응답 = FAIL |
| **컨펌 없이 본실행** | 핑퐁·리허설·작업계획 = 컨펌 대기 |
| **완성형 입력 함정** | 사용자가 DSL·블록·템플릿 완성본 줘도 작업계획·리허설 hit = Before/After·dry-run 의무. 완성도와 컨펌게이트 무관. §5-2 |
| **즉시실행 동사 압력** | "붙여줘·적용해줘·수정해줘" 명령형이 컨펌게이트 트리거와 동시 등장 → 컨펌게이트 우선. 동사 강도가 대기를 압도 = FAIL. §5-1 |
| **단방향 선형 스킬과 충돌** | up-manager·skill-builder INIT 직행 = FAIL. 컨펌게이트 5단계 먼저 출력 → 컨펌 → INIT 진입. §5-1 |
| **NOT 항목 발동** | TRIZ·이쁘니는 라우팅. 본 사전 콤보 적용 ✗ |
| **시스템리마인더 단독에 invoke** | 사용자 의도 메시지 ✗ → 생략 |

---

## §8. 미발동 신고 (changelog)

발동 누락·오작동 발견 시 즉시 기록:

- **경로:** `VAULT/Agent-Ops/_autoloop-lab/trigger-skill/changelog.md`
- **포맷 (1줄):** `YYYY-MM-DD · 누락트리거 · 메시지요약 · 원인추정`

autoloop이 다음 변이에 반영. 누락은 박제해야 진화한다.

---

## §9. 설계 헤리티지

본 스킬은 **trigger-dictionary v1.1 HARD-FIRE (4/23) 발동 강제력**과 **v3.7 glossary 게이트 (4/26)**의 결합. 5세대 진화에서 검증된 5대 기법:

1. **정식명 전수 + 조사변주를 description P1에 박제** (v1.1)
2. **"미발동 = FAIL" 박제 + 판단금지 마커** (v1.1)
3. **조사·어미 변주 명시** (v1.1)
4. **비대칭 비용 프레이밍** (v3.2)
5. **응답 직전 자기점검 체크리스트** (v3.2)

여기에 v3.7의 **glossary 강제 Read 게이트 (활용 verbatim 보증)**를 추가해 발동률 100% + 정의 활용 100%를 동시 보증.

**v1.1 (2026-04-28):** 컨펌게이트 우선권(§5-1) + 완성형 입력 함정(§5-2) + 자체점검 컨펌게이트 검사 2번 승격. UP에 DSL 블록 붙이는 작업에서 작업계획 컨펌 누락 → INIT 직행 사례 박제.
